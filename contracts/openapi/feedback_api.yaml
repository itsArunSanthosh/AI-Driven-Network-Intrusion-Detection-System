openapi: 3.0.0
info:
  title: Inference API
  version: 1.0.0

paths:
  /predict:
    post:
      summary: Get prediction for a flow
      requestBody:
        required: true
      responses:
        '200':
          description: Prediction result